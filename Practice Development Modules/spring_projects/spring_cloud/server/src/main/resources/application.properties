
# Server does not need to register with eureka nor fetch registry information
# Need to set it as false explicitly because it will be set as true by default
eureka.client.register-with-eureka = false
eureka.client.fetch-registry = false

# Default port number
server.port = 8761

# Application name
spring.application.name = Amazon - Server